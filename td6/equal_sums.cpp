#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
int main() {
    ll T;
    cin >> T;
    for(ll i=0; i<T; i++) {
        ll N;
        cin >> N;
        vector<ll> vec(N);
        unordered_map<ll, ll> mp;
        for(ll j = 0; j<N; j++) {
            cin >> vec[j];
        }
        ll cum_sum = 0;
        vector <ll> cumulative;
        vector<vector<ll>> perms;
        ll counter = 0;
    }
    return 0;
}
